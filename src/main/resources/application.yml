spring:
  profiles:
    active: Mariadb # 기본 데이터베이스 환경 선택
---
spring:
  profiles: Mariadb
  datasource:
    url: jdbc:mariadb://218.237.179.69:3306/test_user_01
    username: test_user_01
    password: dl945pa
    driver-class-name: org.mariadb.jdbc.Driver

  # HTTP 인코딩 (HttpEncodingProperties)
  http:
    encoding:
      charset: UTF-8 # the encoding of HTTP requests/responses
      enabled: true # enable http encoding support
      force: true # force the configured encoding

  mustache:
    suffix: .html