<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ordinary.repository.mapper.UserMapper">

    <select id="findUserByLoginId" resultType="com.ordinary.repository.dao.UserDao">
        SELECT *
        FROM user
        WHERE login_id = #{loginId}
    </select>

    <insert id="SetSignUp" parameterType="com.ordinary.repository.dao.UserDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user
        (user_type, login_id, user_name, password, user_tel, user_email, user_firstdate, user_logindate, user_ipaddress)
        VALUES
        (#{param.userType}, #{param.loginId}, #{param.userName}, #{param.password}, #{param.userTel}, #{param.userEmail}, now(), #{param.userLoginDate}, #{param.userIpAddress})
    </insert>

</mapper>