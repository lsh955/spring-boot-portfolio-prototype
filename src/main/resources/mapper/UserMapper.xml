<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ordinary.repository.mapper.UserMapper">

    <select id="loadAllData" resultType="com.ordinary.repository.dao.UserDao">
        SELECT type,
               level,
               state,
               email,
               name,
               tel,
               password,
               joinDate,
               deleteDate,
               loginDate,
               logOutDate,
               ipAddress
        FROM user
        WHERE email = #{email}
    </select>

    <select id="loadAllEmail" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE email = #{email}
    </select>

    <insert id="inputSignUp" parameterType="com.ordinary.repository.dao.UserDao" useGeneratedKeys="true" keyProperty="idx">
        INSERT INTO user
        (type,
         level,
         state,
         email,
         name,
         tel,
         password,
         joinDate,
         deleteDate,
         loginDate,
         logOutDate,
         ipAddress)
        VALUES (#{userDao.type},
                #{userDao.level},
                #{userDao.state},
                #{userDao.email},
                #{userDao.name},
                #{userDao.tel},
                #{userDao.password},
                now(),
                null,
                null,
                null,
                #{userDao.ipAddress})
    </insert>

    <select id="findEmail" resultType="String">
        SELECT email
        FROM user
        WHERE tel = #{tel}
    </select>

    <select id="findPassword" resultType="String">
        SELECT password
        FROM user
        WHERE email = #{email}
    </select>

    <update id="inputLoginDate" parameterType="string">
        UPDATE user
        SET loginDate = (now())
        WHERE email = #{email};
    </update>

    <update id="inputLogOutDate" parameterType="com.ordinary.repository.dao.UserDao">
        UPDATE user
        SET logOutDate = (now())
        WHERE email = #{userDao.email};
    </update>

    <update id="inputDeletDate" parameterType="com.ordinary.repository.dao.UserDao">
        UPDATE user
        SET deleteDate = (now())
        WHERE email = #{userDao.email};
    </update>

</mapper>