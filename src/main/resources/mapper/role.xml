<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.portfolio.mapper.RoleMapper">
    <resultMap id="Role" type="com.springboot.portfolio.dto.Roles">
        <result property="id" column="role_id"/>
        <result property="role" column="role"/>
    </resultMap>
    <select id="getRoleInfo" resultMap="Role">SELECT role_id, role FROM role
        <where>
            <if test=" role != null and role != '' ">AND role = #{role}</if>
        </where>
    </select>

    <resultMap id="UserRole" type="com.springboot.portfolio.dto.UserRole">
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
    </resultMap>
    <insert id="setUserRoleInfo"
            parameterType="map"> INSERT INTO user_role ( user_id, role_id ) VALUES ( #{param.userId}, #{param.roleId} ); </insert>
</mapper>