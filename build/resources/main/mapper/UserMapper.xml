<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ordinary.repository.mapper.UserMapper">

    <select id="getFindUserByLoginId" resultType="com.ordinary.repository.dao.UserDao">
        SELECT *
        FROM user
        WHERE login_id = #{loginId}
    </select>

    <select id="getIdCheck" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE login_id = #{loginId}
    </select>

    <insert id="setSignUp" parameterType="com.ordinary.repository.dao.UserDao" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user
            (user_type, login_id, user_name, password, user_tel, user_email, user_firstdate, user_logindate, user_ipaddress)
        VALUES
            (#{userDao.userType}, #{userDao.loginId}, #{userDao.userName}, #{userDao.password}, #{userDao.userTel}, #{userDao.userEmail}, now(), #{userDao.userLoginDate}, #{userDao.userIpAddress})
    </insert>

    <select id="getFindId" resultType="String">
        SELECT login_id
        FROM user
        WHERE user_email = #{userEmail}
    </select>

    <select id="getFindPassword" resultType="String">
        SELECT password
        FROM user
        WHERE login_id = #{loginId}
    </select>

</mapper>